---
name: Bug report
about: Report unexpected behavior in the workflow CLI, parser, validator, or emitters
title: ""
labels: bug
---

## What happened
<!-- Expected vs. actual. Include the failing command and its output if relevant. -->

## Reproduce
<!-- Minimal steps. Include the relevant `*-states.json` snippet and the command you ran. -->

1.
2.
3.

## Environment

- OS and version (`uname -a` on Linux, `sw_vers` on macOS):
- Python version (`python3 --version`):
- `workflow` version (`pip show workflow` / `uv pip show workflow`):
- Install method: [pip / uv / pipx / source]
- Backend: [GitHub / other]

## Output
<!-- Paste relevant command output (`--json` where available). Redact anything you don't want public. -->

```
```
