# Won't-fix note — `<issue-title>`

Attached when advancing `refining → wont_fix` (gate: `wont_fix`, audit-level by default).

## Why this isn't fixed
<One short paragraph. Categorize: not-a-bug / out-of-scope / superseded / cost-too-high / waiting-on-upstream.>

## What the reporter should do instead
<Workaround, alternative product, related ticket, or "nothing — accept the behavior".>

## Reversibility note
Reopening this issue is cheap. If a fresh report lands with new context (frequency,
severity, new data), reopen and re-refine rather than treating this close as final.
