"""workflow — canonical operation mechanism for agent-driven state-machine workflows.

This package implements the framework defined in `agent-workflow-skill-creator`.
The public surface is the CLI (see workflow.cli) and the WorkflowBackend protocol
(see workflow.backends.base).
"""

__version__ = "0.1.0"
